services:
  sshd:
    build:
      context: .
      dockerfile: docker/test-sshd/Dockerfile
    ports:
      - "2222:2222"
    healthcheck:
      test: ["CMD-SHELL", "pgrep -x sshd >/dev/null"]
      interval: 2s
      timeout: 2s
      retries: 15
